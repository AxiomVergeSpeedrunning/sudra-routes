const urls = {
  home: '/',
  tutorials: '/tutorials/',
  routes: '/routes/',
  race: '/race/',
  // Holding off on this for now as it might be a bit harder to implement
  // streams: '/streams/',
};

export default urls;
