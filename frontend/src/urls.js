const tRoot = '/tutorials/';

const urls = {
  home: '/',
  tutorials: {
    root: tRoot,
    create: `${tRoot}create/`,
    view: `${tRoot}view/`,
    edit: `${tRoot}edit/`,
  },
  routes: '/routes/',
  race: '/race/',
  streams: '/streams/',
  dictionary: '/dictionary/',
  login: '/login/',
  register: '/register/',
  account: '/account/',
};

export default urls;
